# Cove v1.3.0

发布日期：2026-09-13。Windows 客户端及服务器版本：1.3.0；Android 独立版本仍为 0.5.2，本次不发布手机版。

本次更新聚焦麦克风发送链路、音频设备兼容性和交互细节，并修复网格拖拽排序问题。

## 语音与音频

- 修复在设置页、语音成员条等界面调节自己的麦克风发送音量时，其他人听感几乎不变的问题：音量现在作用于实际上行 WebRTC 音轨；处理链异常时会自动恢复可调音轨。
- 增加可选的 RNNoise 实验降噪，默认仍使用系统降噪；模型初始化或运行失败时安全回退，不会发布未经回声处理或降噪保护的音轨。
- 优先请求全系统回声消除，并对不支持该能力的设备给出兼容性提示；设备切换、静音、强制禁言与音量状态保持一致。
- 补充音频处理、WebRTC/Opus 回环、回声消除和降噪回退测试。

## 交互与显示

- 修复网格拖拽排序在卡片间隙和不同抓取位置下的落位误判。
- 设置页新增主题化的麦克风降噪选择控件；优化深色主题、屏幕共享设置和语音包标题。
- 消息提醒不再把人员进出播报和语音包记录当作普通新消息；补全聊天消息头像显示。

## 更新与发布

- 自动更新固定优先使用 GitHub，Gitee 作为回退源。
- 桌面客户端与服务器版本统一升级到 1.3.0。

本次没有数据库结构变更，已有房间、聊天记录和语音包可直接沿用。
