# Cove v1.2.0

发布日期：2026-09-12。Windows 客户端及服务器版本：1.2.0；Android 独立版本：0.5.2。

本次更新集中打磨远程控制与语音恢复的可靠性，重做共享屏幕下的聊天区动画，补齐语音包与成员管理的日常操作，并修复一批深色主题下的显示问题。

## 远程控制

- 本机操作优先：只要本机用户正在移动鼠标，就丢弃此后 400 毫秒内的远程鼠标输入并立即释放已按下的鼠标键，避免远控与本机操作互相打架。
- 远程输入改为有界队列：合并相邻的鼠标移动、同一时刻只保留一条待确认指令、过期指令直接丢弃；输入积压或组件无响应时立即停止控制，而不是继续堆积。
- 修复长按按键卡住的问题：当每秒输入次数达到上限时，按键与鼠标按钮的“抬起”事件仍然放行。
- 授权失败时给出具体原因。此前共用一句“本机远程输入组件不可用”，现在会说明是紧急停止快捷键被其他程序占用、输入组件缺失，还是这次请求已被新的请求取代。
- 控制会话的授权状态不再依赖界面渲染顺序：断开连接立即吊销本机控制，迟到的回调无法恢复已经结束的会话。

## 语音与共享音频

- 断线恢复策略显式化：信令断开但收发媒体通道仍然连通时，保留正在进行的语音，不会因为一次迟到的重连事件中断通话；确认服务端会话未恢复时才清理媒体。
- 共享音频改为登记式播放调度，延迟到达的历史音频不再一次性涌入扬声器，修复由此产生的爆音与延迟累积。
- 修复服务端媒体诊断中往返时延被重复放大 1000 倍的显示错误。

## 屏幕共享与聊天

- 观看共享时，聊天区改为固定宽度整体平移开合，不再逐帧重新排版消息气泡与图片，消除拉出聊天区的卡顿。
- 屏幕共享设置按影响范围重新排序：分辨率 → 以原生分辨率共享 → 帧率 → 游戏模式。
- 聊天图片大图查看支持滚轮缩放与拖动平移，双击还原，点击空白处关闭。
- 聊天链接识别支持不带协议头的裸域名，同时正确排除邮箱地址与文件路径，中文标点不会被并入链接。

## 语音包

- 语音包卡片支持右键唤出操作菜单：下载到本地、重命名、删除。下载保留音频的原始格式与扩展名，不上转成 mp3，也不使用非法文件名。
- 拖拽排序重写：带缓动与错峰的位移动画、支持键盘操作、拖动期间光标不再在抓握手与指针之间闪烁。

## 房间与界面

- 房主可右键成员唤出操作菜单，执行语音禁言或移出房间；成员列表与共享态成员条均可使用，被移出的成员会立即离开房间。
- 修复浅色与深色房间背景互相覆盖的问题：只提交用户实际编辑过的那一套主题，另一套沿用服务器上的原值。
- 所有浮窗支持点击外部关闭；个人名片加入淡入动画；登录与注册切换时固定卡片上边缘，不再上下同时扩张导致按钮位移。
- 修复深色主题下的多处显示问题：证书开关的说明文字对比度、说话头像绿框内部的白圈、登录页历史按钮左边缘的白边。
- 结束共享按钮的文字颜色改为与麦克风、共享音频一致的色相。

## 更新与发布

- 更新中心在空闲、已是最新、更新服务不可用等状态下不再留下空白卡片。
- 主进程忽略标准输出断管（EPIPE），开发或测试环境下日志管道关闭不再弹出“main process error”。
- 手机版新增发布闸门：打标签后先校验 Android `versionCode` 严格大于线上清单，构建完成后用最终 APK 复核更新清单与 SHA-256，并确认更新说明与应用内说明同源；同时修复了此前已分发更新说明的编码乱码。

## 升级说明

- Windows 桌面客户端和服务器版本均为 `1.2.0`。Release 提供两者安装包、对应 blockmap 和客户端 `latest.yml` 自动更新清单。
- 本次没有数据库结构变更，已有房间、聊天记录与语音包均可直接沿用，无需迁移。
- 请同时更新服务端与使用中的客户端。更新云端 Node 服务时请替换编译后的服务端代码并重启 `cove-server`，保留原数据目录；客户端更新不会自动升级云端服务端。
- 远程控制仍仅支持 Windows 桌面客户端，双方都需要支持该功能的客户端。共享者的紧急停止快捷键为 `Ctrl+Alt+Shift+X`；如果该快捷键被其他程序（包括另一个正在运行的 Cove）占用，申请控制时会明确提示，请关闭占用该快捷键的程序后重试。
- 手机端采用独立版本号，已发布 APK 仍为 `0.5.2`。本次推送包含手机端源码改动，但不发布新的 APK。
