# Cove v0.5.3

本次版本修复 Windows 桌面端在服务器地址不可用时无法退出连接界面的问题。

## 修复

- 初次连接服务器增加 30 秒硬性截止，超时后主动停止重连。
- 连接失败界面始终提供“取消连接并修改地址”，无需等待超时即可更换服务器。
- 连接超时后自动显示服务器地址输入框，可直接保存并重新连接。
- Socket.IO 连接错误和身份注册失败不再让客户端永久停留在加载状态。

## 改进

- 登录页和连接失败界面增加 `localhost` 使用范围提示，避免远程用户误用本机地址。
- 新增连接截止回归测试，并将全部桌面端测试纳入 GitHub Release 工作流。

## 版本说明

- 桌面客户端与服务器版本更新为 0.5.3。
- Android 手机端仍为 0.3.1，本次未重新发布。
- 已安装 0.5.2 的 Windows 客户端可通过内置自动更新器获取本版本。
