# Cove v0.7.0

本次中版本更新集中改进聊天、语音成员交互、屏幕共享画质与双平台自动更新。

## 新增

- 聊天消息自动识别 HTTP/HTTPS 超链接，以蓝色下划线显示并通过系统默认浏览器安全打开。
- 聊天输入框支持多行文本，`Enter` 发送、`Shift+Enter` 换行。
- 自动更新同时支持 GitHub Releases 与 Gitee Releases，并按版本与网络响应自动选源、失败自动回退。

## 改进与修复

- 语音包只向当前语音成员播放；仅在频道中的成员仍可看到播放记录，但不会听到音频。
- 重新实现加入/离开语音提示：服务端精确选择语音受众、事件去重，并在聊天区显示 `[用户] 加入/离开了语音`。
- 成员接收音量与实时说话强度改为 Windows 音量合成器风格的同轴单轨控件，实时电平受音量上限约束。
- 提高屏幕共享带宽上限，1080p 动态画面最高允许 6 Mbps；mediasoup 初始可用发送带宽提升到 4 Mbps。
- 屏幕共享优先使用 AV1 编码，设备不支持时自动回退 VP9 或浏览器默认编码。
- 修正共享调试统计，将发送端/当前观看流的码率、帧率与丢包分别绑定到正确通道。

## 版本说明

- 桌面客户端与服务器版本更新为 0.7.0。
- Android 手机端仍为 0.3.1，本次不重新发布。
- GitHub 与 Gitee 均提供相同的 Windows 客户端、服务器安装包及更新清单。
