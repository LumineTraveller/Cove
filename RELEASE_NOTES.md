# Cove v1.5.1

Windows 客户端与服务端版本：1.5.1。Android 版本单独编号；本次不发布新 APK，当前 APK 仍为 0.6.0（versionCode 10）。

## 语音连接与稳定性

- 媒体通道暂时断开时不再立刻启动退房清理；允许通道自行恢复。持续的终态传输失败仍会在宽限期后清理，避免留下失效媒体资源。
- 信令短暂中断时，只要媒体通道还处于可恢复状态，就保留当前语音会话。
- 屏幕共享启动错误、系统音频共享错误和语音包删除失败均只影响对应功能，不会因为局部错误离开语音。

## 屏幕共享

- Windows 屏幕采集不再在取得采集轨道后重复应用帧率约束，避免采集源被重启后报错或出现黑屏。
- 调整共享模式时只修改发送端编码参数，不对正在运行的 Windows 采集源施加新的采集约束。
- 共享音轨结束后及时清理观看端对应轨道，避免保留已结束的音轨。

## 连接诊断

- 暂时无法连接服务器时展示 Socket.IO 断开/连接错误原因；恢复期间保留原因信息，成功重连后清除。

本次没有数据库结构变更；现有账号、房间、聊天记录和语音包可直接沿用。Gitee 仅同步 Git 仓库，不创建 Gitee Release。
