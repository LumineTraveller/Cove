# Cove v0.8.0

本次中版本更新新增 Windows 11 单应用音频共享，并收紧语音包播放权限、简化退出房间提示，同时提供更精细的音量调节。

## 新增

- 可从当前桌面应用列表中选择一个应用，仅共享该应用及其子进程的声音，不发送屏幕画面、Cove 通话或其他系统声音。
- 应用音频拥有独立的发送音量控制和清晰的共享状态，可随时停止。
- 增加按服务器来源启用的 HTTPS 证书例外，例外只对用户配置的 HTTPS 主机与端口生效。

## 改进与修复

- 只有已经加入语音的成员才能同步播放语音包；频道内但未加入语音的成员无法播放。
- 取消退出房间时的提示音和聊天播报，保留退出语音提示。
- 语音包音量调节步长由 5% 缩小为 1%。
- 房主麦克风禁言不再影响成员正在发送的独立应用音频轨道。

## 版本说明

- Windows 桌面客户端与服务器更新为 0.8.0。
- Android 手机端仍为 0.3.1，本次不重新发布。
- GitHub 与 Gitee 均提供相同的 Windows 客户端、服务器安装包及自动更新文件。
