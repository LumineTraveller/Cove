# Cove v1.5.0

Windows 客户端与服务端版本：1.5.0。Android 版本单独编号；本次同步手机版源码更新，但不发布新 APK，当前 APK 仍为 0.6.0（versionCode 10）。

## 聊天与交互

- 聊天区复制动图时保留 GIF 动画，同时提供 PNG 回退；粘贴 GIF 可继续作为动图发送。
- 输入框右键菜单提供撤销、重做、剪切、复制、粘贴和全选。
- 语音包删除改为直接删除，不再弹出额外确认；桌面与手机版的个人备注名称显示保持一致。

## 屏幕共享与音频

- 屏幕画面与系统音频使用同一媒体同步组，并由共享画面统一播放；观看端音量和输出设备设置会应用于共享音频。
- 共享选择阶段跳过未使用的窗口缩略图采集，缩短开始共享前的等待。

## 用户资料与服务端

- 桌面和手机版在成员、房主、共享者、聊天播报及语音包信息中统一显示用户备注。
- 服务端向客户端提供稳定的公开用户标识，并将聊天作者标识加入可兼容迁移；不暴露内部账号标识，不清除已有数据。
- Cove 服务端公开提供 `/downloads` 和 `/releases` 文件路由。Linux 默认目录为 `/var/www/cove-download`，也可用 `COVE_DOWNLOAD_DIR` 指定。

## 更新来源

- 移除 Gitee 更新下载入口。自动更新通过当前选择的服务器检查，并以 GitHub 为回退来源；手动服务器下载链接跟随客户端当前填写的服务器地址。
- Gitee 仅同步代码与更新清单，不创建 Gitee Release。Windows 客户端和服务端安装包发布到 GitHub Release，并镜像到 Cove 服务器下载目录。

## 手机版源码

- 更新检查与下载支持当前配置的 HTTPS 服务器，并保留 GitHub 来源；手机版更新页面也移除了 Gitee 下载选项。
- 成员、房主、共享者、聊天和语音包显示用户备注时保持一致。

已有账号、房间、聊天记录和语音包可直接沿用。服务器访问密码门禁仍默认关闭。
