# Cove v0.10.2

发布日期：2026-09-01。Windows 客户端及服务器版本：0.10.2；Android 独立版本：0.5.2。

## 房间设置

- 双端创建房间时可选择人数上限或不限人数，并设置可选密码；人数包含房主和 7.5 秒恢复窗口内的成员。
- 房主可修改上限、更换或取消密码。降低上限只限制新成员加入，修改密码不会踢出现有成员。
- 入房时验证密码和容量，并防止同时抢占最后一个名额导致超员；密码以加盐摘要保存，不向客户端公开。
- 有密码房间的历史消息仅在成功进入房间后读取。

## 账号与语音修复

- 同账号只允许一个在线会话：新登录立即使旧凭据失效、退出旧端的房间、语音、共享和远控，并停止旧端自动重连。
- 普通网络恢复不会被视为新登录。同一凭据的重复在线连接不会相互抢占。
- 异常断线保留 7.5 秒恢复窗口；超时后清理媒体残留，可直接重新加入语音，无需重新连接服务器。
- 修复断线期间缓存退出指令、媒体初始化失败及迟到请求造成的重入异常。
- 主动退出、踢人及远控中断仍立即处理。

## 升级说明

- 请同时更新服务端及使用中的客户端，云端服务端不会随客户端自动升级。
- 数据库自动补充房间设置字段；已有房间默认不限人数、无密码，原房间和聊天数据保留。
- 旧数据库同账号的多份登录凭据仅保留最新一份，其余设备需重新登录。
- 更新云端 Node 服务前建议备份数据目录，更新后重启 `cove-server`。
- Windows Release 提供客户端和服务器安装包；Android APK 在独立的 `mobile-v0.5.2` Release 中。
