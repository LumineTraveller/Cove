# Cove v1.2.1

发布日期：2026-09-12。Windows 客户端及服务器版本：1.2.1；Android 独立版本仍为 0.5.2，本次不发布手机版。

本次更新聚焦共享屏幕交互、聊天布局和主题显示的稳定性。

## 屏幕共享

- 修复观看共享时“全屏”按钮无法点击的问题：透明悬停桥不再覆盖状态栏按钮。
- 宽窗口下共享聊天区作为固定列平滑开合，窄窗口下才使用覆盖式抽屉，减少聊天内容重排和开合卡顿。
- 游戏模式锁定帧率时，帧率选项同步禁用并显示锁定状态。

## 聊天与主题

- 聊天图片右键菜单和大图查看器移到窗口顶层，避免共享布局的 transform 导致位置偏移或被裁剪。
- Portal 菜单显式继承房间主题颜色，保证自定义房间背景下的可读性。
- 修复深色房间消息气泡和自己发送消息的文字对比度；清理已失效的旧消息选择器。
- 聊天输入框字号跟随消息字号设置。

## 资源与发布

- 压缩内置应用图标资源，减少安装包体积。
- 桌面端与服务端版本统一升级到 1.2.1。

本次没有数据库结构变更，已有房间、聊天记录和语音包可直接沿用。
