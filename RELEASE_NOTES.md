# Cove v0.10.1

发布日期：2026-08-31

本次更新优化共享屏幕观看操作，并加入跨服务器的登录记忆。Windows 桌面客户端版本为 0.10.1，手机端独立版本为 0.5.1。

## 新增

- 远程操控：观看共享屏幕时可发起申请，只有共享者明确同意后才允许发送鼠标、键盘及滚轮输入。请求会自动超时，双方均可结束控制；共享者可使用 `Ctrl+Alt+Shift+X` 紧急停止。停止共享、离开频道或断开连接时同步清理控制会话。
- 屏幕共享新增 1440p（2K）档位，支持最高 60 FPS；保持画面比例、不裁剪，也不会放大低于档位尺寸的窗口。
- 在线成员与频道成员显示电脑/手机小图标。设备类别由客户端上报，未上报的旧客户端不显示图标，不猜测设备类型。
- 登录页记住最近使用的服务器和账号。有效会话可以一键恢复，切换服务器会保留会话；退出账号或会话失效时清除令牌但保留地址和邮箱，不保存明文密码。
- 观看共享屏幕时，观看音量和远程控制申请/停止操作统一放入共享横幅，避免悬浮控件重叠。
- 屏幕共享设置新增“以原生分辨率共享”，勾选后直接使用采集源尺寸并禁用清晰度档位；取消后恢复此前档位。

## 改进

- 540p、720p、1080p、1440p 的自动模式与游戏模式不再设置固定码率上限；模式切换时清除旧上限，保留分辨率、帧率设置及 WebRTC 拥塞控制。
- 取消桌面端麦克风的 32 kbps 上限，以及屏幕音频、应用音频的 96 kbps 上限。
- 服务端初始可用发送带宽、桌面共享发送端及 VP8 回退的启动估计统一为 10 Mbps。它是启动估计，不是固定上限，也不保证始终占满带宽。
- 更新共享设置说明和媒体诊断面板，明确显示“不设上限”，同时保留实际码率、编码器目标及可用带宽信息。
- Windows 客户端从 Electron 29 升级至 Electron 44.0.0。
- 安装包使用最高压缩，仅保留中英文 Electron 语言资源，并移除已被 Vite 打包的重复前端依赖、开发映射和类型声明文件，降低下载体积。
- 右键成员卡片可打开房主管理菜单，提供禁言/解除禁言和移出房间；常驻成员栏按钮已移除，移出仍需确认。

## 升级说明

- Windows 桌面客户端和服务器版本均为 `0.10.1`。Release 提供两者安装包、对应 blockmap 和客户端 `latest.yml` 自动更新清单。
- 远程操控目前仅支持 Windows 桌面客户端，双方需要新版客户端和新版服务端；这不是无人值守远程访问，不会自动接受控制请求。
- 设备图标需要新版服务端和新版客户端共同支持；查看旧客户端用户时不会显示设备图标。
- 手机端采用独立版本号，本次发布 APK 为 `0.5.1`，包含双指缩放和登录记忆。
- 更新云端 Node 服务时请替换编译后的服务端代码并重启 `cove-server`，保留原数据目录。客户端更新不会自动升级云端服务端。
