# Cove v0.8.1

发布日期：2026-08-28

本次更新聚焦共享媒体调试、全屏体验、音量控制和应用内反馈，并移除不再需要的本地诊断日志写入。

## 新增

- 共享屏幕调试信息增加精简模式，共享方显示实际发送帧率，接收方显示接收帧率；仅保留帧率、分辨率和媒体码率。
- 增加覆盖整个显示器的完全全屏共享模式，并改进退出后恢复原始布局的逻辑。
- 共享音频接收者可以独立调节音量；共享屏幕和共享音频横幅支持收回与还原。
- 成员栏新增共享屏幕、共享音频状态标识。

## 改进与修复

- 移除调试共享屏幕问题时的本地日志文件写入，避免正常使用产生无用日志。
- 优化应用内更新提示，明确显示检查、下载、校验和安装阶段。
- 自身语音电平条增加麦克风图标，与其他成员左对齐；点击其他成员扬声器图标即可本地静音或恢复音量。
- 本地音量调节范围扩大至 0–200%，100% 为原始音量，超过 100% 可放大声音，并修复远端语音音量调节不生效的问题。
- 房主移除成员及被移除成员的提示统一使用应用内窗口样式。

## 版本说明

- Windows 桌面客户端与服务器更新为 0.8.1。
- Android 手机端仍为 0.4.0，本次不重新发布。
- GitHub 与 Gitee 均提供相同的 Windows 客户端、服务器安装包及自动更新文件。
