# Cove v1.4.1

状态：待发布。Windows 客户端及服务器版本：1.4.1；Android 独立版本源码为 0.6.0（versionCode 10）。

本次更新聚焦远程控制安全延迟、共享界面显示和聊天区/语音包交互优化，同时保留默认关闭的服务器访问密码兼容能力。

## 本次修复与优化

- 远程控制请求增加 10 秒授权延迟、倒计时和取消入口，连接断开时不会误启动控制。
- 共享屏幕标题和成员条优先显示用户备注，找不到备注时回退到用户名。
- 修复语音包面板跟随控制球展开/收起导致定位跳动的问题。
- 优化聊天区展开/收起动画，减少消息重新折行造成的卡顿。

## 语音与音频

- RNNoise 改为默认麦克风降噪，系统降噪保留为兼容回退选项。
- RNNoise 模型 WASM 改为独立按需加载资源，减少安装包中的重复编码体积。
- 改进全系统回声消除、设备约束失败说明和处理链回退提示。
- 修复语音包片段试听结束后按钮不恢复的问题。

## 交互与显示

- 登录页会先识别服务器安全状态，仅在新服务器且初始化凭据可用时显示一次性凭据输入框。
- 已记住账号遇到未初始化、令牌过期或密码轮换时，会返回服务器解锁界面并显示明确原因。
- 服务器历史记录区分同一 IP 上的不同端口和路径。

## 服务器访问安全

- 加入独立于账号密码和房间密码的服务器访问密码，服务端只保存带盐哈希。
- 加入一次性初始化凭据、12 小时访问令牌、失败限速、密码轮换和旧令牌撤销。
- 公网启用门禁后要求 HTTPS/WSS，并拒绝不支持当前访问协议的旧客户端。
- 门禁默认关闭；本版本用于先把兼容客户端部署给用户，管理员确认覆盖后再显式启用。

## 更新与发布

- 自动更新同时探测当前服务器与 GitHub；同版本优先当前服务器，服务器镜像落后时使用较新的 GitHub 版本。
- 桌面客户端与服务器版本统一升级到 1.4.1。

已有房间、聊天记录和语音包可直接沿用。服务器安全表会自动创建，但门禁保持默认关闭。
