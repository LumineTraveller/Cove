# Cove 房间页 UI 重构计划

> 这份文档整理了 Cove 从旧版房间界面迁移到 Scheme 2 新 UI 时确定的方案，便于转发和评审。
> 目标是最大限度保持设计稿的布局、层级、交互和动效；未覆盖的页面继续使用旧版，不在本次范围内重写。

## 一、目标与边界

### 目标

- 将房间内的成员、共享屏幕、共享音频、聊天和控制栏统一到 Scheme 2 的视觉与交互体系。
- 保留现有语音、聊天、屏幕共享、音频共享、语音包、远程控制和房间管理能力。
- 让普通状态、待观看状态、观看/共享状态之间有明确且平滑的布局过渡。
- 每个房间拥有独立的头像和背景渐变；根据背景明暗自动选择文字与图标颜色。
- 旧客户端仍可连接新服务端；新增字段尽量采用可选字段，避免破坏兼容性。

### 明确不在本次范围内

- 登录、注册、服务器连接、更新下载、首次启动等未被新 UI 覆盖的页面继续保留旧版。
- 不新增“屏幕来源/窗口来源”选择按钮，也不实现该功能。
- 本次只迁移桌面端房间页；移动端 UI 另行评审，不能因桌面端迁移而自动重写。
- 不在 UI 迁移阶段顺便改变带宽策略、编解码策略或账号规则。

## 二、功能差异对比

| 功能区域 | 旧版 Cove | Scheme 2 目标 | 客户端需要做的事 | 服务端需要做的事 |
| --- | --- | --- | --- | --- |
| 应用导航 | 房间和成员入口较分散 | 左侧窄栏/宽栏两态；顶部软件图标，下面是房间头像，底部是设置 | 新建导航栏、展开/收回曲线动画、房间切换 | 房间列表返回头像、在线语音人数和房主标识 |
| 房间头像 | 默认图标，缺少房间级设置 | 房主可设置房间头像，未设置时显示房间图标 | 房间头像展示、上传/输入头像地址的设置入口 | 保存并广播 `avatarUrl` |
| 房间外观 | 全局或固定背景 | 每个房间独立的顶部/底部颜色，两端胶囊色带预览并形成渐变 | 颜色编辑、实时预览、自动对比度 | 保存并广播 `backgroundTop`、`backgroundBottom` |
| 房间设置 | 入口和成员栏耦合 | 房主齿轮打开二级菜单：头像、名称、人数、密码、删除 | 二级菜单、表单、权限态和反馈 | `room:update-settings` 校验房主；支持名称、人数、密码和外观字段 |
| 普通成员区 | 成员纵向排列，音量控件占比偏大 | 自己置顶；头像、麦克风状态、设备角标、共享图标、成员音量和共享音频音量分层显示 | 新成员卡片；音量范围 0–200；滚轮步长 1；悬停浮窗显示百分比 | 继续提供成员语音/共享状态；补充客户端平台标识（桌面/移动） |
| 共享待观看 | 共享状态提示与成员列表耦合 | 中部略拓宽；“观看共享”按钮固定在共享者条目的最右侧 | 不改变原成员布局，只增加右侧动作位 | 共享生产者状态继续通过现有事件广播 |
| 观看/共享状态 | 视频、成员、聊天布局切换不稳定 | 聊天自动收起；上方为矮成员栏，下方为视频区和状态条 | 三态布局动画；聊天按钮；视频窗口、状态条、控制层 | 保持现有媒体生产/消费协议 |
| 共享屏幕设置 | 有游戏模式、分辨率、帧率等设置 | 完整保留旧选项，并增加“原生分辨率”；不提供屏幕/窗口来源选择 | 新白色工具弹窗；原生分辨率勾选后禁用分辨率档位 | 继续接受现有编码参数与共享状态 |
| 共享电脑音频 | 与共享屏幕流程容易混在一起 | 与屏幕共享独立；可选全部系统音频（排除 Cove）或某个进程 | 独立浮窗；系统音频和进程音频入口 | 继续使用独立音频生产者/消费者 |
| 成员音量 | 传统水平条，百分比常驻 | 普通状态水平条；共享音频有独立条；百分比仅悬停浮窗 | 统一 0–200、滚轮调节、静音/闭麦按钮、悬停定位 | 无新增持久化要求，保留客户端本地音量 |
| 底部功能条 | 控件较多且层级不明确 | 居中悬浮：麦克风、共享屏幕、共享音频、语音包、调试、离开 | 图标状态色、窄栏时仅显示图标、弹出层定位和动效 | 复用现有 socket/media 事件 |
| 聊天 | 右侧聊天区 | 普通状态聊天区占主体且不能被误唤出空白栏；观看共享时自动收起，可覆盖视频展开 | 聊天抽屉、滑动动画、消息刷新不改变主布局 | 复用历史消息与实时消息协议 |
| 远程控制 | 已有请求/确认能力 | 观看他人共享时状态条内显示；必须由共享者确认 | 请求、确认、拒绝、停止和紧急停止 UI | 复用远程控制请求/会话/输入事件并校验权限 |
| 全屏 | “全屏”与“窗口全屏”语义容易混淆 | “全屏”占满电脑屏幕；“应用全屏”占满 Cove 窗口；均可用 Esc 退出 | Fullscreen API 与应用 CSS 全屏分别实现 | 无需改动 |
| 成员菜单 | 菜单位置固定，成员栏内按钮拥挤 | 在点击位置附近打开；房主禁言/移除操作放入右键/二级菜单 | 锚点定位、边界翻转、房主权限显示 | 继续校验禁言/移除权限 |
| 个人资料 | 头像/改名入口不稳定 | 个人头像、改名、成员资料保留 | 资料弹窗与成员菜单入口 | 复用现有用户资料字段 |

## 三、服务端字段与协议清单

### 新增或需要确认的房间字段

| 字段 | 类型 | 用途 | 兼容策略 |
| --- | --- | --- | --- |
| `avatarUrl` | `string \| null` | 房间头像 | 可选；为空时客户端使用默认房间图标 |
| `backgroundTop` | `string \| null` | 房间背景顶部颜色 | 可选；非法值回退默认色 |
| `backgroundBottom` | `string \| null` | 房间背景底部颜色 | 可选；非法值回退默认色 |
| `maxMembers` | `number \| null` | 房间人数上限，空值表示不限 | 保留原有默认行为 |
| `password` | `string \| null` | 房间密码设置 | 服务端保存哈希或现有安全形式，客户端不回显明文 |
| `clientPlatform` | `desktop \| mobile` | 成员设备角标 | 只接受白名单值，旧客户端缺失时显示通用图标 |

### 需要保留或扩展的事件

- `rooms:get`：返回房间摘要，并按当前 socket 返回 `isOwner` 等当前用户相关信息。
- `room:state`：在现有成员、共享状态之外携带房间名称和外观字段。
- `room:create`：接受可选的名称、人数上限和密码。
- `room:update-settings`：只允许房主修改名称、人数、密码、头像和背景颜色。
- 现有屏幕共享、共享音频、聊天、远程控制和语音包事件保持兼容。
- 新增字段均为附加字段；旧客户端忽略未知字段即可继续运行。

## 四、新版 UI 覆盖程度

### 由 Scheme 2 覆盖并迁移的页面/状态

| 页面或状态 | 覆盖程度 | 说明 |
| --- | --- | --- |
| 房间内窄栏/宽栏导航 | 完整 | 房间头像、语音人数气泡、设置入口、房间切换 |
| 普通成员状态 | 完整 | 成员卡片、平台/麦克风/共享标识、音量交互 |
| 有人共享但尚未观看 | 完整 | 观看按钮位于成员条最右侧，不打乱原布局 |
| 正在观看他人共享 | 完整 | 矮成员栏、视频区、状态条、聊天抽屉、远程控制 |
| 自己正在共享 | 完整 | 共享设置、共享状态条、结束共享、调试信息 |
| 底部控制条 | 完整 | 麦克风、共享、语音包、调试、离开 |
| 成员菜单和房主操作 | 完整 | 以点击位置为锚点，房主操作置于二级菜单 |
| 房间外观/房间设置 | 完整 | 房间头像、名称、人数、密码、双端渐变颜色 |
| 共享屏幕设置弹窗 | 完整 | 游戏模式、原生分辨率、分辨率、帧率、共享电脑音频 |
| 独立共享音频弹窗 | 完整 | 全部系统音频或应用进程音频 |
| 资料与应用设置 | 完整/复用 | 个人资料和全局音频、服务器、更新等入口保留能力 |
| 调试信息 | 完整 | 观看/共享状态下可在视频左上角展开 |

### 保留旧版、不在本次重构的页面

| 页面或流程 | 处理方式 |
| --- | --- |
| 登录/注册 | 保留旧版；只复用现有认证逻辑 |
| 服务器地址、证书策略和连接失败页 | 保留旧版；不因房间 UI 迁移而改变连接流程 |
| 房间列表/首次进入流程 | 保留旧版作为入口；进入房间后切换到新版房间页 |
| 更新检查、下载、安装准备和发布提示 | 保留旧版；不与房间 UI 混合 |
| 首次启动、异常提示和系统级窗口流程 | 保留现有实现 |
| 移动端页面 | 单独规划，不自动套用桌面端布局 |

## 五、分阶段实施路线

### 阶段 0：冻结基线

1. 复制当前版本到独立副本或分支。
2. 记录旧版房间页、登录页、更新页和连接流程的可回滚状态。
3. 建立 UI 状态清单：普通、待观看、观看他人共享、自己共享、聊天展开/收回。

### 阶段 1：以设计稿建立 UI 骨架

1. 将 Scheme 2 原型的布局、间距、圆角、阴影、动效和图标作为唯一视觉基准。
2. 提取 CSS 变量：背景、面板、文字、强调色、危险色、边框、阴影和动画曲线。
3. 先做静态壳层，不接业务逻辑，确认像素级布局后再接数据。

### 阶段 2：接入房间导航与外观

1. 实现窄栏/宽栏展开动画和房间切换。
2. 接入房间头像、语音人数气泡和房主齿轮。
3. 接入房间设置与双端颜色编辑。
4. 实现颜色对比度检测，让文字和图标在浅色/深色背景上均可识别。

### 阶段 3：接入普通成员区

1. 以“自己置顶、其他成员稳定排序”为基础渲染成员卡片。
2. 加入麦克风状态、设备平台角标、屏幕/音频共享状态。
3. 加入成员音量和共享音频音量条，支持 0–200 和滚轮步长 1。
4. 百分比只在拖动或悬停时以浮窗显示，不常驻成员栏。

### 阶段 4：接入共享状态转换

1. 实现待观看状态，观看按钮固定在共享者条目右侧。
2. 实现观看/共享状态的上下分区布局与过渡动画。
3. 聊天区在观看共享时自动收起，展开后以覆盖层方式出现，不挤压视频区。
4. 将音量、远程控制、全屏和结束操作统一放入视频状态条。

### 阶段 5：接入控制栏和弹出层

1. 实现底部悬浮功能条的图标状态色和窄/宽模式。
2. 所有菜单按触发控件位置定位，禁止先出现在右侧再闪现到目标位置。
3. 成员菜单使用点击位置作为锚点，并在屏幕边缘自动翻转。
4. 共享屏幕、共享音频、语音包和调试信息使用统一的浮窗容器。

### 阶段 6：接入真实媒体与系统能力

1. 接入旧版 WebRTC 生产者/消费者，不改变媒体协议。
2. 屏幕设置保留游戏模式、原生分辨率、分辨率档位和帧率档位。
3. 屏幕共享与共享电脑音频解耦；共享音频提供系统音频和进程音频入口。
4. 接入应用全屏、系统全屏和 Esc 退出。
5. 接入远程控制请求、确认、拒绝、停止和紧急停止。

### 阶段 7：兼容性与质量验证

1. 验证旧版登录、房间列表、更新页和连接页未发生视觉或行为回归。
2. 使用旧客户端连接新服务端，确认可加入房间、语音和共享能力正常。
3. 验证桌面端不同窗口尺寸、浅色/深色背景、聊天消息刷新和滚动。
4. 验证键盘焦点、Esc 退出、减少动画偏好和边缘菜单定位。
5. 通过构建、单元测试、媒体测试和手工双端验收后，再合入主线。

## 六、验收标准

- 普通状态下聊天区占主体且始终可见；收到消息不会凭空出现空白聊天栏，也不会把主界面向左顶开。
- 待观看状态只在共享者条目最右侧增加“观看共享”，成员布局与普通状态一致。
- 观看共享时聊天默认收起，控制条和状态条不遮挡视频；成员条居中，悬停音量浮窗窄且稳定。
- 全屏占满整个电脑屏幕；应用全屏只占满 Cove 窗口；两者都能按 Esc 退出。
- 共享屏幕设置不出现屏幕/窗口来源按钮；共享电脑音频可独立开关，且排除 Cove 自身音频。
- 每个房间的背景颜色、头像和设置互不串房，刷新或重新进入后仍能恢复。
- 所有成员/共享音量控件支持 0–200、滚轮步长 1，百分比不常驻，只在悬停或拖动时显示。
- 远程控制始终需要共享者明确确认，拒绝或结束后不能继续发送输入。
- 旧版登录、注册、服务器连接、更新下载等未覆盖页面保持可用。

## 七、发布与回滚原则

- UI 迁移应在独立副本或分支完成，验收通过后再合并。
- 服务端字段采用增量迁移，发布前保留数据目录和可回滚备份。
- 在正式发布前保留旧房间页入口或可切换开关，出现严重回归时可立即回退。
- 测试构建、临时安装包和本地截图不进入仓库；发布包在验收完成后再生成。

