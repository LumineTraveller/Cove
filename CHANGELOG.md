# Cove 更新日志

本文件记录每次正式版本更新。桌面客户端与服务器使用同一版本号，手机端单独编号。

## 桌面端 0.9.0 - 2026-08-29

### 新增

- 登录 Cove 现在需要注册账号：使用邮箱和密码注册并登录后，房主权限、语音包归属和个人资料在重装或更换设备后依然保留。

### 改进与修复

- 修复共享屏幕包含系统音频时共享失败的问题。
- 优化调节其他成员音量的界面。
- 观看共享屏幕时，聊天框的展开与收起添加了过渡动画。
- 改善了操作按钮的整体样式。
- 优化共享者一端的共享屏幕音量调节界面。
- 共享屏幕界面的全屏按钮、横幅隐藏半圆和音量调节控件提高透明度，观感更轻盈。
- 提高共享屏幕调试信息面板的图层，避免被其他悬浮控件遮挡。
- 每次退出频道后，会清除此前的语音包播放记录以及成员进出频道/语音的播报，重新进入不再恢复这些历史内容。
- 修复自己退出语音时没有提示音的问题。
- 优化 Gitee 下载模式，修复更新包超额下载的问题。

### 版本说明

- 桌面客户端与服务器版本更新至 `0.9.0`；手机端保持 `0.4.0`。
- GitHub 与 Gitee Release 同步提供 Windows 客户端、服务器安装包以及客户端自动更新文件。

## 桌面端 0.8.2 - 2026-08-29

### 新增与改进

- Windows 屏幕共享的系统音频改用排除 Cove 进程树的原生采集，避免把 Cove 自己的语音、语音包和提示音回录给其他人；增加独立的音频采集生命周期管理。
- 观看共享时只在视频图层显示当前共享者横幅，默认收回且不再额外占用布局；支持停止观看后切换其他共享，并保留多个共享者的状态。
- 共享接收音量改为紧凑的悬浮扬声器控件，悬停或聚焦后显示纵向调节栏，支持 1% 步长、键盘上下调节和 0–100% 范围。
- 调整成员栏共享屏幕/应用音频标识的位置，使其与语音电平条对齐且不改变原有成员信息结构；成员接收音量支持滚轮按 1% 调节。
- 语音包支持一次选择多个文件导入，按顺序上传并显示进度；对非音频、超大文件和单个上传失败提供汇总提示。
- 共享者端隐藏无意义的完全全屏入口；观看者端修正全屏菜单悬停、退出和恢复逻辑。

### 版本说明

- Windows 桌面客户端与服务器版本更新至 `0.8.2`；手机端保持 `0.4.0`。
- GitHub 与 Gitee Release 同步提供 Windows 客户端、服务器安装包以及客户端自动更新文件。

## 桌面端 0.8.1 - 2026-08-28

### 新增与改进

- 共享屏幕调试信息增加精简模式，共享方显示实际发送帧率、接收方显示接收帧率，仅保留帧率、分辨率和媒体码率；同时移除本地调试日志写入功能。
- 优化应用内更新提示，明确区分检查、下载、校验和安装阶段。
- 自身语音电平条增加麦克风图标并与其他成员左对齐，支持点击其他成员扬声器图标本地静音/恢复；本地音量范围扩大至 0–200%，修复远端语音音量调节不生效的问题。
- 增加覆盖整个显示器的完全全屏共享模式，改进完全全屏退出和窗口恢复逻辑。
- 将房主移除成员及被移除成员的反馈改为应用内窗口提示。
- 共享音频接收者可独立调节音量；优化共享屏幕、共享音频横幅，并增加收回/还原交互。
- 在成员栏显示共享者的屏幕和音频状态标识。

### 版本说明

- Windows 桌面客户端与服务器版本更新至 `0.8.1`；手机端保持 `0.4.0`。
- GitHub 与 Gitee Release 同步提供 Windows 客户端、服务器安装包以及客户端自动更新文件。

## 桌面端 0.8.0 - 2026-08-27

### 新增

- Windows 11 桌面端支持选择一个正在运行的应用，仅共享该应用及其子进程的声音，不包含 Cove 通话、屏幕画面或其他系统声音。
- 应用音频共享提供独立发送音量控制，并可在语音工具栏中随时停止。
- 增加针对当前服务器地址的 HTTPS 证书例外开关；例外严格限定到用户配置的 HTTPS 主机与端口，不影响其他站点。

### 改进与修复

- 语音包同步播放仅允许语音中的成员发起；仅在频道内的用户仍可上传和管理，但播放卡片会明确禁用。
- 移除退出房间时的提示音和聊天播报，退出语音的提示保持不变。
- 语音包音量改为 1% 步长，支持更精细的本机音量调节。
- 房主禁言只暂停成员麦克风，不会误伤独立的应用音频共享轨道。

### 版本说明

- 桌面客户端与服务器版本更新至 `0.8.0`；手机端保持 `0.3.1`。
- GitHub 与 Gitee Release 同步提供 Windows 客户端、服务器安装包以及客户端自动更新文件。

## 桌面端 0.7.1 - 2026-08-26

### 改进与修复

- 移除加入房间时的重复音效和聊天播报，加入语音的音效与播报保持不变。
- 成员列表中始终将当前用户置于首位，其余成员按“语音中优先、仅在房间内随后”排序。
- 修复房主管理按钮压缩其他成员电平条的问题，所有成员的音量/实时强度轨道统一右对齐。

### 版本说明

- 桌面客户端与服务器版本更新至 `0.7.1`；手机端保持 `0.3.1`。

## 桌面端 0.7.0 - 2026-08-26

### 新增

- 聊天消息自动识别 HTTP/HTTPS 超链接，以蓝色下划线显示并通过默认浏览器打开。
- 聊天输入框支持多行内容，使用 `Shift+Enter` 换行。
- 自动更新增加 Gitee Releases 下载源，与 GitHub 并行探测、择优下载并在失败时自动切换。

### 改进与修复

- 语音包音频仅发送给语音中的成员，频道成员仍可看到播放记录。
- 修复加入语音播报未生效的问题；加入者和当前语音成员可听见提示音，聊天区同步显示加入/离开语音记录，重复事件会去重。
- 将成员接收音量与实时语音强度合并为同一中心线上的单轨控件，并按接收音量限制实际显示电平。
- 提升共享带宽上限与 mediasoup 初始带宽估计，优先使用 AV1，并自动回退 VP9 或浏览器默认编码。
- 修正屏幕共享调试统计绑定错误，避免把非当前共享、语音或本机发送带宽混入观看端数据。

### 版本说明

- 桌面客户端与服务器版本更新至 `0.7.0`；手机端保持 `0.3.1`。
- GitHub 与 Gitee Release 同步提供 Windows 客户端、服务器安装包以及客户端自动更新文件。

## 桌面端 0.6.0 - 2026-08-25

### 新增

- 增加 Windows 收音设备与扬声器选择，并在本地记忆用户选择。
- 聊天图片支持点击选择、拖拽到聊天区和粘贴到输入框发送。
- 房主可以将成员移出房间，被移除用户会退出语音并返回频道列表。
- 个人名片中的版本号改为检查更新入口，更新状态、下载进度与安装操作统一显示在应用内。

### 改进

- 进入频道时聊天区默认定位到最新消息，修正图片消息头像与消息顶部的对齐关系。
- 修复主页面在线列表、频道人数、房间成员与语音人数偶尔不同步的问题。
- 合并成员的实时说话电平与接收音量控件，改为无缝叠放的双层轨道。
- 成员列表按“语音中优先、仅在房间内随后”实时稳定排序。
- 增加入房、离房聊天播报并提高提示音音量。
- 主页面使用 Cove 图标替换文字标识，移除无实际用途的网络和设置入口。
- 优化主页面空状态文案，将“创建第一个频道”统一为“创建一个频道”。

### 版本说明

- 桌面客户端与服务器版本更新至 `0.6.0`；手机端保持 `0.3.1`。

## 桌面端 0.5.3 - 2026-08-25

### 修复

- 修复初次连接到无效服务器时，客户端可能永久停留在加载遮罩且无法修改地址的问题。
- 初次连接增加 30 秒硬性截止；超时后主动停止 Socket.IO 重连并进入服务器地址编辑状态。
- 连接错误或身份注册失败时不再锁死界面，用户可以立即取消连接、修改地址并重新连接。

### 改进

- 在登录页和连接失败界面明确说明 `localhost` 只适用于服务器所在电脑。
- 为连接截止、成功取消计时和手动取消补充回归测试，并纳入桌面端发布工作流。
- 桌面客户端与服务器版本更新至 `0.5.3`；手机端保持 `0.3.1`。

## 桌面端 0.5.2 - 2026-08-25

### 新增

- 增加 Windows 客户端自动更新器，启动后自动检查 GitHub Releases，并每四小时重新检查。
- 发现新版后在后台自动下载，通过任务栏显示进度，下载完成后可立即重启安装或稍后处理。
- 增加独立更新日志文件，记录检查、下载和安装错误，便于定位更新问题。
- 增加 GitHub Actions 发布流程，通过 `v*.*.*` 标签自动构建并发布 Windows 安装包。

### 改进

- 更新失败不会阻止 Cove 登录、语音聊天或屏幕共享。
- 发布产物统一包含 `latest.yml`、NSIS 安装包和 blockmap 差分更新文件。
- 桌面客户端与服务器版本更新至 `0.5.2`；手机端保持 `0.3.1`。

## 桌面端 0.5.1 / 手机端 0.3.1 - 2026-08-25

### 更新

- 为 Cove 桌面客户端、服务器程序、Android 和 iOS 添加统一的品牌图标。
- 补齐 Windows 多尺寸 ICO、Android 各屏幕密度及圆形启动图标、iOS AppIcon 资源。
- 桌面客户端与服务器版本更新至 `0.5.1`，手机端更新至 `0.3.1`。

## 桌面端 0.5.0 / 手机端 0.3.0 - 2026-08-24

### 新增

- 增加独立手机端应用，支持加入房间、麦克风控制、播放语音包和观看屏幕共享。
- 增加用户主页、头像、用户名及好友备注功能。
- 聊天区支持发送图片，并显示语音包播放记录。
- 增加成员独立音量记忆、语音包音量调节、改名、删除及顺序调整。
- 增加屏幕共享“观看共享”控制、共享音量调节和游戏模式。
- 增加房主身份、成员禁言和删除房间功能。

### 改进与修复

- 合并房间中的成员与语音列表，以语音条和状态高亮展示语音成员。
- 修复多人同时共享时，后发共享覆盖先发共享状态的问题。
- 优化屏幕共享码率、帧率和无人观看时的资源占用。
- 改进 Windows 麦克风处理、底噪抑制及重复音频轨问题。
- 手机端支持息屏保持麦克风、默认扬声器播放和选择是否观看共享。
- 增加加入与退出语音提示音，并同步成员开关麦状态。

## 桌面端 0.2.0 - 2026-08-23

### 更新

- 建立 Cove 桌面客户端与服务器的版本化发布基线。
- 完成客户端 UI、个人名片和语音包界面的首轮重构。
